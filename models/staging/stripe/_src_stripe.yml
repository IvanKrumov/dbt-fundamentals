sources:
  - name: stripe
    database: jaffle-shop-476509 # in BigQuery we use the project id instead the database in Snowflake
    schema: stripe
    tables:
      - name: payment
        config:
          loaded_at_field: _batched_at
          freshness:
            warn_after: {count: 12, period: hour}
            error_after: {count: 24, period: hour}